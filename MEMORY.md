# Memory

## SESSION START PROTOCOL

1. Read `HANDOFF.md` for orientation (who is the user, what state is the repo in)
2. Scan `HOT_CONTEXT.md` for current priorities
3. If `Intent.md` exists, read it -- that is the north star for the current project
4. Only deep-read skill files if the task requires them

## SESSION END PROTOCOL

Before ending after meaningful work, offer to run `/handoff` to save session state.
This updates HANDOFF.md and HOT_CONTEXT.md so the next session picks up where this one left off.
