Save session state so your next session picks up where you left off.

Follow the handoff skill at `.cursor/skills/handoff/SKILL.md`:

1. Update `HANDOFF.md` with current state, story, decisions, next steps, warnings
2. Update `HOT_CONTEXT.md` with what matters right now
3. Append to `NARRATIVE.md` if something significant happened
4. Tell the user what you saved
